# Minimal dependencies required to build the docs on Read the Docs.
# Keep this list focused so RTD doesn't install unrelated dev/test tooling.
sphinx>=8.0,<9.0
myst-parser>=4.0,<5.0
sphinx-book-theme>=1.1.4,<2.0.0
